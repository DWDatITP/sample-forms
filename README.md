# Examining forms

## Tools

This uses the following tools and libraries:

  * express
  * express.static
  * body-parser
  * util

## Install

  * `git clone`
  * `cd sample-forms`
  * `npm install`
  * `nodemon server.js`
  * visit <http://localhost:5000>
